export interface Employee {
    id?: number;
    name?: string;
    description?: string; 
}

export interface People {
    userId?: number | undefined;
    id?:number | undefined;
    title?:string;
}